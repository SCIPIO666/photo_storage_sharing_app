const prisma=require('../config/prismaConfig')
const logger=require('../utils/logger')
async function createDefaultFolders(){
try {
    const userAvatarFolder= await prisma.
} catch (error) {
    
}
}